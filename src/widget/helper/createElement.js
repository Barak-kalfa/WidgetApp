// This is a template for creating a new recommendation type
// just add your own center element in the "rec-thumb" div and add its type and options to the settings json file.
// You can edit this entire recommendation element to your choosing.

// export function createElement(rec, element) {
//   const RecElement = document.createElement(element.elementType);
//   RecElement.classList.add(element.className);
//   RecElement.id = rec.id;
//   RecElement.innerHTML = `
//         <a href="${rec.url}" target="_blank">
//           <div class="rec-thumb">
            
//          ENTER CENTER ELEMENT HERE

//           </div>
//           ${element.title ? `<div class="rec-title">
//           <p>${rec.name}</p>`: ""}
//           </div>
//           ${element.footer ? `<div class="rec-footer">
//             <span>${rec.branding}</span>
//             <p>${rec.origin}</p>
//           </div>` : ""}
//           </a>
//   `;
//   return RecElement;
// }
